# Stack

`Stack` arranges children in a single axis with consistent token-backed spacing.

```tsx
<Stack spacing="4">
  <h2>Settings</h2>
  <Button>Save changes</Button>
</Stack>
```

It is vertical by default. Set `direction="horizontal"` for a horizontal arrangement.

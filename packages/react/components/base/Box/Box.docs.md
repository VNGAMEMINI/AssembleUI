# Box

`Box` is a neutral block container that preserves all native `div` props.

```tsx
<Box className="profile-section" aria-label="Profile details">
  Content
</Box>
```

Use `Box` when you need a semantic grouping without imposing a layout direction or spacing.

# Flex

`Flex` lays out children with flexbox using token-backed gap values.

```tsx
<Flex align="center" justify="between" gap="4">
  <span>Title</span>
  <Button>Save</Button>
</Flex>
```

`direction` defaults to `row`, `align` to `stretch`, `justify` to `start`, and `gap` to `0`.

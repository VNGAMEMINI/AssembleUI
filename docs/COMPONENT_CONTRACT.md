# AssembleUI Component Contract

## Purpose

Every AssembleUI Component must follow a predictable structure,
dependency direction, export model, styling model, and test contract.

## Directory Structure

Every Component uses:

```text
ComponentName/
├── ComponentName.tsx
├── ComponentName.types.ts
├── ComponentName.scss
├── ComponentName.test.tsx
└── index.ts
